<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.b7tpm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailRedFormActivity"></activity>
        <activity android:name=".DetailWhiteFormActivity" />
        <activity android:name=".RCARedFormActivity" />
        <activity android:name=".RCAWhiteFormActivity" />
        <activity android:name=".MenuRCAActivity" />
        <activity android:name=".InformasiTPMActivity" />
        <activity android:name=".NewRedFormActivity" />
        <activity android:name=".NewWhiteFormActivity" />
        <activity android:name=".NewFormActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>